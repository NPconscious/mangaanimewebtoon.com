---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-flex items-center justify-center bg-canvas-soft text-body text-[12px] font-normal leading-[16px] px-xs rounded-full border border-hairline ${className}`}>
  <slot />
</span>
